# 섹터로테이션 개편 설계

> 2026-08-24 시작. 공시캘린더의 섹터로테이션 화면에서 외국인 수급 로직을 걷어내고, jhts 가설을 빌려 쓰는 구조로 바꾸는 작업 문서. 확정된 내용과 미결 사항을 여기 쌓는다. 코드는 여기서 합의된 뒤에 고친다.

&nbsp;

---

## 지향점 (목표)

섹터로테이션 화면은 지금 시장에서 돈이 어느 테마·섹터로 도는지 한눈에 보여주는 도구다. 이번 개편의 목표는 그중 **외국인 수급을 gongsi가 자체적으로 판단하지 않고, jhts에서 연구·검증한 가설을 그대로 빌려와 판정하는 것**이다.

외국인 수급을 어떻게 봐야 하는지는 jhts 리서치팀이 투자 서적에서 가설을 뽑아 검증하는 주제다. gongsi가 따로 매집 로직을 만들면 두 곳의 기준이 어긋난다. 그래서 **jhts를 수급 판단의 단일 출처로 두고, gongsi는 그 가설을 시장 전체 종목에 적용해 섹터 단위로 보여주는 소비자**가 된다.

바뀐 화면은 이런 질문에 답한다. "지금 이 가설(예: 외국인 압축 매집)이 어느 섹터의 종목들에서 신호를 내고 있는가."

&nbsp;

---

## 설계 원칙

1. **수급 판단은 jhts가, 표시는 gongsi가.** gongsi는 새 점수식이나 매집 판별을 발명하지 않는다. 가설이 낸 신호를 그대로 받아 섹터로 묶어 보여준다.
2. **근거를 함께 보여준다.** 종목이 신호를 냈으면 어떤 가설의 어떤 조건을 충족했는지(지표값 포함)를 화면에 그대로 편다. jhts 설계 원칙 1번(사람이 검수할 수 있어야 한다)과 같은 태도다.
3. **판정 계산은 jhts 엔진과 100% 일치시킨다.** gongsi가 지표를 다시 구현하지 않고 jhts 엔진을 그대로 불러 쓴다. 같은 데이터면 두 프로젝트가 같은 판정을 낸다.
4. **가설 반영은 사람 명령으로만.** jhts가 가설을 바꾸거나 새로 만들어도 gongsi가 자동으로 따라가지 않는다. 어떤 가설을 쓸지는 사용자가 지정하고, 바뀔 때도 사용자가 지시한다.

&nbsp;

---

## 무엇을 바꾸나 (기존 → 개편)

### 걷어내는 것 (외국인 수급 자체 로직 전량 폐기)
- `supply.py`: 매물대(POC)·매물대 상단 아래 여부·매집일수(accum_days)·자체 선행/후행(lead/lag) 판별 전체
- `sectors.py`: 외국인·기관 순매수 부착(`investor_trend`), `netbuy_pct`, `_apply_supply`, `inflow`·`inflow_score`, relvol≥2 기반 lead/lag 카운트

### 남기는 것 (섹터 골격)
- 대상 종목 수집, 테마·섹터 분류, ETF 클러스터, 섹터별 구성종목 로스터
- relvol(상대거래대금)·turnover(회전율)·등락률 같은 시세 지표

### 새로 넣는 것
- 사용자가 지정한 jhts 가설을 종목별로 판정(jhts 엔진 호출)
- 섹터별로 "가설 신호가 난 종목"을 집계해 표시·정렬

&nbsp;

---

## 대상 종목 (universe)

- **코스피·코스닥 전 종목을 대상으로 한다.** (ETF·ETN 제외, 비보통주 제외)
- 기존의 "거래대금 상위 800" 제한은 폐기한다. `market_universe.fetch_all()`이 이미 전 종목을 한 번에 받아오므로, 상위 N으로 자르지 않고 전체를 쓴다.
- jhts의 대상 종목 범위(코스피·코스닥 전 종목)와 맞춘다. 두 프로젝트가 같은 종목 위에서 같은 가설을 돌려야 판정이 어긋나지 않는다.

> 참고: 전 종목으로 넓히면 종목별 일봉·외국인 일별 데이터를 그만큼 더 받아야 한다. 조회량·캐시 전략은 구현 단계에서 잡는다(미결).

&nbsp;

---

## jhts 가설 연동 구조

### 가설의 형태 (`~/jhts/hypotheses/H###-*.md`)
가설 하나가 md 파일 하나다. 사람이 읽는 문서이면서 기계가 파싱하는 스펙이다. 매수 조건은 yaml로 적힌다.

```yaml
name: H002
entry:                          # 조건 여러 개는 전부 AND
  - {ind: frgn_uptrend_ratio, window: 30, op: ">=", value: 0.7}
  - {ind: bandwidth_pct, window: 30, op: "<=", value: 7.0}
  - {ind: return_pct, window: 30, op: ">=", value: -3.0}
```

- 청산 조건은 없다(jhts 공통 규칙 소관). gongsi는 매수 조건 충족 여부만 쓴다. "지금 이 종목이 이 가설이 말하는 자리인가"를 판정한다.
- 표준 지표로 표현 안 되는 가설은 `커스텀:` 서술이라 자동 판정이 안 된다. gongsi도 그런 가설은 대상에서 뺀다.

### 현재 등록된 외국인 수급 가설
| 가설 | 매수 조건 요지 | 필요 데이터 | gongsi 판정 |
|---|---|---|---|
| H002 외국인 누적 우상향+가격 횡보 | frgn_uptrend_ratio≥0.7 & 좁은 횡보 | 외국인 일별 순매수 + 일봉 | 가능 |
| H003 저점 V자 회복+외국인 유입 | vu_recovery 20~30% & frgn_uptrend_ratio≥0.6 | 외국인 일별 + 일봉 | 가능 |
| H004 외국인 순매수 연속성 | frgn_net_streak≥3 | 외국인 일별 순매수 | 가능 |
| H001 보유율 가속 3일연속 | hold_ratio_accel_streak≥3 & … | 외국인 보유비율(일별) | 보류(데이터 없음) |

H001은 외국인 보유비율 일별 데이터가 필요한데 gongsi엔 순매수만 있다. 보유비율 계열 가설은 데이터 확보 전까지 보류한다.

### 지표 엔진 (import 재사용)
- `jhts/strategies/spec.py`의 `SpecStrategy(spec).scan(code, candles, position, extras)`가 스펙대로 AND 판정을 하고, 충족하면 근거(reason)를 붙인 신호를 돌려준다.
- `jhts/strategies/base.py`의 `INDICATORS`가 지표 계산 함수 모음이다.
- gongsi는 `sys.path`에 `~/jhts`를 더해 이 둘을 그대로 불러 쓴다. 지표를 다시 구현하지 않는다.

### gongsi가 공급할 데이터 (엔진 입력 계약)
- `candles`: `List[Candle(date, open, high, low, close, volume)]` 일봉 — `quotes.daily_price`를 open·high·low까지 나오도록 확장
- `extras`: `{"flows": [{date, frgn_net_value, market_cap, ...}]}` 외국인 일별 순매수 — `quotes.frgn_daily`(약 40일)
- 즉 gongsi가 종목별로 (일봉 + 외국인 일별)만 만들어 넣으면, 판정은 jhts 엔진이 그대로 해 준다.

&nbsp;

---

## 섹터 신호 정의

- **종목 단위**: 사용자가 지정한 가설 중 하나라도 충족하면 그 종목은 "신호 종목"이다. 어떤 가설을, 어떤 지표값으로 충족했는지를 함께 남긴다.
- **섹터 단위**: 그 섹터의 구성종목 중 신호 종목이 얼마나 나오는지로 표시하고 정렬한다.
- 기존 lead/lag·inflow_score는 폐기한다. 새 표시·정렬 기준의 구체 형태(신호 종목 수 / 신호 비율 / 가설별 분해)는 실제 데이터를 보고 확정한다(미결).

&nbsp;

---

## 확정 사항

- 외국인 수급 자체 로직 전량 폐기, jhts 최신 가설을 빌려 판정하는 구조로 전환 (2026-08-24)
- 쓸 가설은 사용자가 지정한 몇 개만 조합해서 적용. gongsi는 가설을 자동 스캔하지 않는다 (2026-08-24)
- 가설 반영은 사용자 명령이 있을 때만. jhts가 가설을 바꿔도 자동으로 따라가지 않는다 (2026-08-25)
- 지표 판정은 jhts 엔진(`SpecStrategy`·`INDICATORS`)을 import해 재사용. 지표 재구현 안 함 (2026-08-24)
- 섹터 신호 = 가설이 낸 신호를 그대로 롤업. gongsi가 새 점수식을 만들지 않는다 (2026-08-24)
- 대상 종목 = 코스피·코스닥 전 종목(거래대금 상위 800 제한 폐기), jhts 대상 범위와 일치 (2026-08-25)
- H001(외국인 보유비율 계열)은 데이터 없어 보류. 초기엔 순매수·일봉 기반 가설만 (2026-08-24)

&nbsp;

---

## 미결 사항

- 섹터 신호의 표시·정렬 방식(신호 종목 수 / 신호 비율 / 가설별 분해) — 실제 데이터로 보고 확정
- 종목 펼쳤을 때 근거 노출 범위(가설명만 / 지표값까지)
- 전 종목으로 넓혔을 때 일봉·외국인 일별 조회량과 캐시 전략
- 사용자가 지정한 가설 ID를 gongsi 어디에 둘지(설정 상수 / 작은 config 파일)
