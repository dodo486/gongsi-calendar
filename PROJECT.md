# 📅 공시캘린더 (DART 실시간 공시 트래커)

> 이 문서는 프로젝트의 **단일 진실 원천(Single Source of Truth)** 입니다.
> 결정, 요구사항, 진행상황을 여기 기록하고 함께 고쳐나갑니다.

---

## 1. 목표 (What we're building)

내가 지정한 **특정 대상(종목/회사)들의 공시만** 골라서
**실시간으로 감지 → 알림/업데이트** 해주는 도구.

- 관심 종목만 타겟팅 (전체 공시 아님)
- 새 공시가 뜨면 바로 잡아냄
- 캘린더/리스트 형태로 정리

---

## 2. 데이터 소스 (DART Open API)

- 제공처: 금융감독원 전자공시 OpenDART — https://opendart.fss.or.kr
- **API 키**: `dart_key.txt` 파일에 저장 (문서에 직접 안 박음)
- 주요 엔드포인트:
  | 용도 | URL |
  |---|---|
  | 공시 검색(목록) | `https://opendart.fss.or.kr/api/list.json` |
  | 기업 고유번호 전체 | `https://opendart.fss.or.kr/api/corpCode.xml` (zip) |
  | 공시 원문 뷰어 | `https://dart.fss.or.kr/dsaf001/main.do?rcpNo={접수번호}` |

### ⚠️ "실시간"의 현실
DART는 **푸시(웹소켓) 없음**. 실시간 = **짧은 주기 폴링(polling)**.
`list.json`을 N초/분마다 호출 → 새 접수번호(rcept_no) 나오면 "신규 공시"로 감지.
→ 실질적 지연: 폴링 주기만큼 (예: 1분마다면 최대 1분 지연).

---

## 3. 감시 대상 (Watchlist)

**코스피200 + 코스닥150 전 종목** → `watchlist.json` (현재 348종목)

- 수시변경(특례편입·상폐·합병 등)으로 아무 때나 바뀔 수 있어 데일리 갱신이 이상적
- 구성종목 출처 현황 (`get_watchlist.py`):
  | 지수 | 소스 | 상태 |
  |---|---|---|
  | 코스피200 | 네이버 금융 (실시간) | ✅ 이 PC에서 정상, 자동 갱신 가능 |
  | 코스닥150 | 임시 스냅샷 `kosdaq150_snapshot.json` (KRX 7/23자) | ⚠️ 임시 — 자동화 TODO |

> ⚠️ **KRX 직접 접근 불가:** 이 PC 네트워크에서 KRX(data.krx.co.kr)는 pykrx·curl_cffi·
> Playwright(헤드리스/헤디드/실크롬) 전부 403/차단됨. 브라우저 수동 접속만 됨.
> → 코스닥150 자동화는 나중에 **실제 크롬 CDP 연결** or **ETF 보유내역**으로 해결 (TODO).

> ⚙️ **폴링 방식 (350개라서 중요):** 종목마다 호출 안 함. `list.json`으로 **당일 전체 공시를
> 받아서** watchlist의 종목코드에 해당하는 것만 로컬 필터링 → API 호출 몇 번이면 끝.

---

## 4. 열려있는 결정들 (Open Decisions) — 골라주세요

### ✅ 확정된 결정 (2026-07-28)
- **실행 형태**: 백그라운드 상주 + 윈도우 알림
- **폴링 주기**: 1분
- **알림 방식**: 윈도우 토스트 알림
- **저장 형태**: JSON 상태파일(중복감지용) + CSV 이력 로그 *(기본값으로 채택, 필요시 변경)*

---

## 4.5 실행 방법 (How to run)

**가장 쉬운 방법: 바탕화면 `공시캘린더` 아이콘 더블클릭** (`launch.py` 실행 = 폴러+웹 한 번에, 이미 켜져있으면 브라우저만)

**부팅 자동실행**: 시작프로그램 폴더에 바로가기 등록됨 → 로그인 시 자동 시작
- 자동실행 끄기: `shell:startup` 폴더에서 `공시캘린더.lnk` 삭제

개별 실행(개발용):
```
python launch.py               # 폴러+웹 한 번에 (아이콘이 이걸 호출)
python get_watchlist.py        # 감시대상 갱신 (코스피200 네이버 + 코스닥150 스냅샷)
python fetch.py 90             # 과거 N일 공시 백필 (증자·배당·자사주 등 9종)
python monitor.py              # 실시간 폴러 상주 (1분마다, 새 공시 윈도우 알림)
python dividends.py 90         # 배당 데이터 추출 (배당금+기준일 원문 파싱) → data/dividends.json
python expiries.py             # 선물·옵션 만기일 생성(규칙 계산) → data/expiries.json
python serve.py                # 웹 캘린더 열기 (http://localhost:8777, 1분 자동갱신)
```
- 관심 공시 종류/키워드: `fetch.py`의 `CATEGORIES` 편집
- 폴링 주기: `monitor.py`의 `POLL_SECONDS` (기본 60초)
- 끄기: 폴러/서버 콘솔 창 닫기(작업표시줄에 최소화됨)

## 5. 진행 로그 (Changelog)

- 2026-07-28: 프로젝트 생성, PROJECT.md 초안 작성
- 2026-07-28: 실행형태/주기/알림 결정 확정 (백그라운드 상주 + 1분 폴링 + 윈도우 알림)
- 2026-07-28: v1 웹 캘린더 완성 (fetch.py / index.html / serve.py) — 공시 자동 표시, 클릭→DART 원문
- 2026-07-28: watchlist.json(348종목) 생성 + fetch.py 필터링 연결 — 코스피200/코스닥150 공시만 표시
- 2026-07-28: KRX 자동접근 불가 확인 → 코스피200 네이버 실시간 / 코스닥150 임시 스냅샷으로 진행
- 2026-07-28: 공시 종류 필터 추가 — **유상증자·무상증자·배당만** (증자=유형B, 배당=유형I). 3개월 백필 169건(배당92/유증68/무증9)
- 2026-07-28: 종류 확장(9종: +자사주·메자닌CB/BW·실적·감자·합병분할·최대주주변경) + 카테고리 분류. 3개월 790건
- 2026-07-28: **실시간 폴러 monitor.py** — 1분 폴링, 새 공시 윈도우 토스트 알림(클릭→DART), 캘린더 자동반영. 웹 1분 자동갱신 추가
- 2026-07-28: launch.py(원클릭 런처, 중복실행 방지) + 바탕화면 아이콘 + 시작프로그램 부팅자동화. serve.py 멀티스레드(ThreadingHTTPServer) 전환
- 2026-07-29: 공시별 날짜 파싱 가능성 조사 — 정형 API(증자·자사주·CB/EB·감자·합병)로 기준일 O, 배당·실적·최대주주변경은 API無(원문 파싱 필요)
- 2026-07-29: **배당 캘린더** — dividends.py(현금ㆍ현물배당결정→배당금, 주주명부폐쇄(기준일)결정→기준일 원문 파싱) + 웹 [공시/배당] 탭. 기준일에 "회사명(금액원)" 표시. 44건
- 2026-07-29: 배당 자동갱신 — monitor.py가 시작 시 + 신규 배당 공시 감지 시 dividends.py 자동 실행(별도 스레드)
- 2026-07-29: **두 캘린더 완전 분리** — 공시 캘린더는 배당 제외(8종, fetch.py `CAL_EXCLUDE`), 배당은 배당 캘린더 전담. 중복 0건. seen.json으로 알림 중복방지 일원화
- 2026-07-29: 배당 탭 사이드에 **"오늘 뜬 배당공시" 리스트** 추가 (dividends.json에 rcept_dt(접수일) 추가, 오늘 접수분 표시·클릭→상세·1분 자동갱신)
- 2026-07-29: **배당 캘린더 표시일 = 잔고확정일(배당기준일 T-2 영업일)** 로 변경. dividends.py에 numpy+holidays로 주말·한국공휴일(근로자의날·연말폐장 포함) 제외 T-2 계산(`confirm_date`). 상세엔 잔고확정일+기준일 병기
- 2026-07-29: **선물·옵션 만기일** 표시 — expiries.py(둘째 목요일 규칙, 3·6·9·12월 동시만기, 휴장시 직전영업일). 두 탭 공통 빨간 마커. monitor 시작 시 자동 생성
- 2026-07-29: 배당 실시간성 개선 — 새 배당 공시 감지 시 전체 재생성(~90s) 대신 **그 1건만 증분 반영(dividends.upsert, ~1s)**, 웹 자동갱신 60→30초. 재시작해도 dividends.json 유지
- 2026-07-30: **안정성 일괄 수리** (코드 리뷰 후속) —
  ① 배당 경합 해결: 전체 재생성 중 도착한 신규 배당은 대기 큐에 보관 후 재생성 완료 시 반영(유실 방지), dividends.py에 쓰기 락 추가
  ② 배당 병합 키 안정화: `배당구분` 정규화(norm_div_type) + 한쪽 파싱 실패 시 유일 엔트리 병합(_match_key)
  ③ disclosures.json 이중 쓰기 해결: monitor는 저장 직전 디스크 재로드 후 병합, fetch.py 백필도 기존 파일과 병합(rcept_no 중복 제거) — 상주 중 백필 안전
  ④ 자정 경계 누락 방지: 폴링 조회 창 오늘→어제~오늘 (중복은 seen이 차단)
  ⑤ TLS: 무검증 폴백을 `DART_INSECURE_TLS=1` 명시적 옵트인으로 변경, 기본은 시스템 인증서 검증
  ⑥ 무한 성장 정리: 공시 이력 365일 보관(RETENTION_DAYS), seen.json 30일 보관, range 실데이터 기준 갱신
  ⑦ 모든 JSON 저장 원자화(tmp→replace) — 웹이 쓰다 만 파일 읽는 사고 방지
  ⑧ 크로스플랫폼: 알림(윈도우 토스트/맥 알림센터/리눅스 notify-send), launch.py 맥·리눅스 대응, fetch.py 날짜 하드코딩 제거, serve.py 127.0.0.1 바인딩, dart_key.txt 없을 때 친절한 안내, 웹 innerHTML 이스케이프(XSS), `import dividends as divmod` 정리

---

- 2026-07-30: **배당 리서치 기능 (research.py)** —
  ① 배당 이력 430일 수집(90일 청크 — DART 3개월 조회 제한 대응) + rcept_no별 원문 파싱 캐시(div_history.json, 신규분만 파싱)
  ② **배당 선진화 자동 판별**: 결산배당 기준일 1~5월(선진화)/12월(구형), 분기·중간배당 기준일 분기말 이탈+공시 후 설정(선진화) → 종목별 verdict+근거
  ③ **예상배당 추정**: 종목·배당구분별 직전 기준일+실측 지급주기(개월)로 다음 기준일·금액 추정(9개월 이내만) → 웹 캘린더 **노란색** 이벤트
  ④ 웹 상세 강화: 배당 클릭 시 "왜 이 날짜인지"(잔고확정일 T-2 설명 + 공시→기준일 순서 + 선진화 판별·근거) 표시, 예상배당 클릭 시 추정 근거 표시
  ⑤ monitor 연동: 시작 시 + 신규 배당 감지 시 리서치 자동 갱신(캐시 기반 증분)

## 6. 다음 할 일 (TODO)

- [x] 결정 4개 확정 (섹션 4)
- [x] Watchlist 생성 (348종목) + 필터링 연결
- [x] v1 웹 캘린더 (공시 자동 표시 + 원문 링크)
- [x] 공시 종류 9종 + 카테고리 분류
- [x] 백그라운드 상주(monitor.py) + 1분 폴링 + 신규 감지 + 윈도우 알림(클릭→원문)
- [x] 웹 1분 자동갱신
- [x] 배당 캘린더 (배당금+기준일 파싱, 웹 탭)
- [x] 배당 데이터 자동 갱신 — 폴러(monitor.py) 시작 시 + 신규 배당 공시 감지 시 자동 재생성
- [x] 실운영용 날짜 자동화 (fetch.py 하드코딩 날짜 → today())
- [x] 안정성 수리 8종 (2026-07-30 로그 참고 — 경합·병합키·이중쓰기·자정경계·TLS·보관정리·원자저장·크로스플랫폼)
- [ ] **코스닥150 자동화** (실제 크롬 CDP or ETF 보유내역) — 현재 임시 스냅샷
- [ ] (선택) 장중 20~30초/장외 느리게 폴링, 카테고리별 색상 구분
